#ifndef COMPARE_FILES
#define COMPARE_FILES

#include "vector.h"

// go through both folders and compare their elements (folders, regular files, links)
// and print their differences
void cmp_folders(const char *dirA, const char *dirB);

#endif // COMPARE_FILES
